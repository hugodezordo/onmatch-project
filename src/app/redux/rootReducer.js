import { combineReducers } from "redux";

// export default combineReducers({
//   counter,
// });

// export const getCounter = (state) => {
//   fromCounter.getCounter(state.counter);
// };

export const rootReducer = combineReducers({});
